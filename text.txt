1. POST input text(line) -> Controller  
2. Controller input text(line) -> Service (Some InputLineProcessor)
...
   Service look for such string (input string from Controller) in the MySQL (equalsIgnoreCase):
      if it present -> search in the MongoDB Document with the same field (text = inputString)
      if doesn't -> does analyses and common logic
...
3. Service inserts input text(line) into MySQl (just line)
4. Servic analyzes tezxt (line) on consonants and vowels and put this model into collection into MongoDB
5. Service returns analysed data (DTO) to Controller
6. Controller returns response to client. 
